import React from 'react';

const AnnotationView = ({match}) => {
  // The kf_id of the file
  const fileId = match.params.fileId;
  return 'annotate your file here'
};

export default AnnotationView;
